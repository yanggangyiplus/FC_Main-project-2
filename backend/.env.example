# ============================================
# MomFlow Backend 환경 변수 설정
# ============================================

# ============================================
# 필수 설정 (로그인/인증)
# ============================================
# Google OAuth Client ID
# Google Cloud Console에서 발급받은 OAuth 2.0 Client ID
GOOGLE_CLIENT_ID=your_GOOGLE_CLIENT_ID

# Google OAuth Client Secret
# Google Cloud Console에서 발급받은 OAuth 2.0 Client Secret
GOOGLE_CLIENT_SECRET=your_GOOGLE_CLIENT_SECRET

# JWT 토큰 시크릿 키
# 프로덕션 환경에서는 반드시 강력한 랜덤 문자열로 변경하세요
# 예: openssl rand -hex 32
JWT_SECRET=your_JWT_SECRET

# ============================================
# CORS 설정
# ============================================
# CORS 허용 오리진 (프론트엔드 URL)
# 여러 개는 쉼표로 구분 (예: http://localhost:5173,https://momflow.com)
CORS_ORIGINS=http://localhost:5173

# ============================================
# 환경 설정
# ============================================
# 실행 환경 (development/production)
ENVIRONMENT=development

# ============================================
# 데이터베이스 설정
# ============================================
# 데이터베이스 연결 URL
# SQLite (기본값): sqlite:///./momflow.db
# PostgreSQL: postgresql://user:password@localhost:5432/momflow
# MySQL: mysql://user:password@localhost:3306/momflow
DATABASE_URL=sqlite:///./momflow.db

# ============================================
# AI 서비스 API 키 (선택사항)
# ============================================
# Google Gemini API Key
# 음성 인식(STT) 기능 사용 시 필요
# https://makersuite.google.com/app/apikey 에서 발급
GOOGLE_GEMINI_API_KEY=your_GOOGLE_GEMINI_API_KEY

# Anthropic Claude API Key
# OCR/이미지 분석 기능 사용 시 필요
# https://console.anthropic.com/ 에서 발급
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ============================================
# 서버 설정 (선택사항 - 기본값 사용 가능)
# ============================================
# 서버 호스트 (기본값: 0.0.0.0)
SERVER_HOST=0.0.0.0
# 서버 포트 (기본값: 8000)
SERVER_PORT=8000
# 서버 자동 리로드 (개발 환경용, 기본값: True)
SERVER_RELOAD=True
# 로그 레벨 (기본값: INFO)
# 옵션: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

#SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=usename@gmail.com
SMTP_PASSWORD=your_SMTP_PASSWORD
SMTP_FROM_EMAIL=username@gmail.com